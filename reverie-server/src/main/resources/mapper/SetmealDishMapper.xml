<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.whurs.mapper.SetmealDishMapper">
    <insert id="insertBatch" parameterType="list">
        insert into setmeal_dish
        (setmeal_id,dish_id,name,price,copies)
        values
        <foreach collection="setmealDishes" item="sd" separator=",">
            (#{sd.setmealId},#{sd.dishId},#{sd.name},#{sd.price},#{sd.copies})
        </foreach>
    </insert>

    <select id="getByDishIds" resultType="java.lang.Long">
        select setmeal_id from setmeal_dish where dish_id in
        <foreach collection="dishIds" separator="," item="dishId" open="(" close=")">
            #{dishId}
        </foreach>
    </select>

    <delete id="deleteBatch">
        delete from setmeal_dish where setmeal_id in
        <foreach collection="setmealIds" item="sid" separator="," open="(" close=")">
            #{sid}
        </foreach>
    </delete>
    <delete id="deleteById">
        delete from setmeal_dish
        <where>
            setmeal_id=#{setmealId}
        </where>
    </delete>

    <select id="getBySetmealId" resultType="com.whurs.entity.SetmealDish">
        select * from setmeal_dish
        <where>
            setmeal_id=#{setmealId}
        </where>
    </select>
</mapper>
